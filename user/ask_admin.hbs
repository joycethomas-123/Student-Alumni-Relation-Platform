<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <style>
        body {
            background-color: #ffffff;
            margin: 0;
            align-items: center;
            justify-content: center;
             background-image: url('/images/logo_transparent1.png');
            background-size: 101% auto;
            background-position: center bottom 40%;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        #uploadForm {
            border-radius: 10px;
            box-shadow: 0 0px 18px rgba(0, 0, 0, 0.4);
            padding: 30px;
            background-color: rgba(231, 236, 238, 0.8);
            transition: background-color 0.3s ease, border-radius 0.3s ease;
        }
        #uploadForm:hover{
            border-radius: 10px;
        }

        .upload_post_submit{
            border-radius: 15px;
            font-weight: bold;
            padding: 15px;
            border: none;
            font-size: 16px;
            margin-top: 15px;
            box-shadow: 0 0px 18px rgba(0, 0, 0, 0.4);
            transition: background-color 0.3s ease;
        }

        .upload_post_submit:hover{
            background-color: #595959;
            color: white;
        }

        .custom-file-upload {
            background-color: #dfe2e4;
            display: inline-block;
            padding: 20px;
            cursor: pointer;
             border-radius: 8px;
            margin-top: 10px;
            box-shadow: 0 0px 18px rgba(0, 0, 0, 0.4);
            transition: background-color 0.3s ease, border-radius 0.3s ease;
        }
        .custom-file-upload:hover{
            
        }

        .input_box{
        border-radius: 20px;
        width: 100%;
        padding: 15px;
        font-weight: bold;
        border: none;
        box-shadow: 0 0px 18px rgba(0, 0, 0, 0.4);
        transition: box-shadow 0.3s ease;
    }

    .input_box:hover{
        box-shadow: 0 2px 18px rgba(0, 0, 0, 0.8);
    }

        .heading{
            font-weight: 550;
            margin-bottom: 15px;
        }

        h2 {
            color: #333;
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-top: 10px;
            font-weight: bold;
        }

        textarea,
        input {
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
       
        .custom-file-upload i {
            margin-right: 5px;
        }

        #imageCarousel {
            text-align: center;
            margin: auto;
        }

        .file_upload_input_box{
            background-color: white;
            width:fit-content;
            margin-bottom:20px;
            border-radius: 20px;
            padding: 15px;
            font-weight: bold;
            border: none;
            box-shadow: 0 0px 18px rgba(0, 0, 0, 0.4);
            transition: box-shadow 0.3s ease;
        }

        .file_upload_input_box:hover{
            box-shadow: 0 2px 18px rgba(0, 0, 0, 0.8);
        }
        .image_view_carosal{
            width: 30px;
        }

         .carousel-inner{
            max-height: 250px;
            width: 100%;
        }

        .video-preview_popup{
            width: 100%;
            height: 88%;
        }
        .image-preview_popup{
            width: 100%;
            max-height: 250px;
        }

         @media (max-width: 768px) {
        body {
            background-image: url('/images/logo1.png');
            background-position: center bottom 40%;
        }
        
         }

         @media (max-width: 767px) {
            .file_upload_input_box{
            width: 340px;
        }
         }
         @media (max-width: 600px) {
            .heading{
            font-weight: 550;
            font-size: 20px;
        }
        .carousel-inner{
            max-height: 250px;
            width: 100%;
        }

        .video-preview_popup{
            width: 100%;
            max-height: 100%;
            object-fit: contain;
        }
        .image-preview_popup{
            width: 100%;
            max-height: 100%;
            object-fit: contain;
        }
        
        }
        @media (max-width: 550px) {
            .file_upload_input_box{
            width: 300px;
        }
        }
        @media (max-width: 500px) {
            .file_upload_input_box{
            width: 250px;
            font-size: 14px;
        }
        }
        @media (max-width: 450px) {
            .file_upload_input_box{
            width: 200px;
            color: white;
        }
        .heading{
            font-weight: 550;
            font-size: 17px;
        }
        }
        @media (max-width: 390px) {
            .file_upload_input_box{
            width: 140px;
        }
        }
         @media (max-height: 942px) and (max-width: 942px) {
       .carousel-inner{
        max-height: 200px;
        width: 100%;
    }

    .video-preview_popup{
        width: 100%;
        max-height: 200px;
    }
    .image-preview_popup{
        width: 100%;
        max-height: 200px;
    }
    }

    @media (max-height: 744px) and (max-width: 744px) {
       .carousel-inner{
        max-height: 170px;
        width: 100%;
    }

    .video-preview_popup{
        width: 100%;
        max-height: 170px;
    }
    .image-preview_popup{
        width: 100%;
        max-height: 170px;
    }
    }
    @media (max-height: 480px) and (max-width: 480px) {
       .carousel-inner{
        max-height: 140px;
        width: 100%;
    }

    .video-preview_popup{
        width: 100%;
        max-height: 140px;
    }
    .image-preview_popup{
        width: 100%;
        max-height: 140px;
    }
    }
    @media (max-height: 458px) and  (max-width: 458px) {
       .carousel-inner{
        max-height: 130px;
        width: 100%;
    }

    .video-preview_popup{
        width: 100%;
        max-height: 100%;
    }
    .image-preview_popup{
        width: 100%;
        max-height: 100%;
    }
    }

    


    </style>
</head>

        <div class="container" style="padding-top: 200px;padding-bottom:200px;">
            <div style="width:100%" class="rows">
                <div id="uploadForm" style="width:100%;">
                    <form id="postForm" action="/ask_admin" method="post" enctype="multipart/form-data">

                        <textarea class="input_box" id="description" style="margin-bottom: 10px;" name="content" rows="8" placeholder="Ask admin" required oninput="resizeTextarea(this)"></textarea>
                        <input type="hidden"  name="admin_opened_time" value="">
                        <input type="hidden"  name="badgeApplicable" value="applicable">

                        <div id="imageCarousel" class="carousel slide" data-ride="carousel">
                            <div class="carousel-inner" id="previewContainer"></div>

                            <a class="carousel-control-prev" href="#imageCarousel" role="button" data-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="carousel-control-next" href="#imageCarousel" role="button" data-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="sr-only">Next</span>
                            </a>
                        </div>

                        <div class="custom-file-upload" style="margin-top: 20px;">
                            <div style="display: block;">
                                <label for="askImage">
                                    <i class="fas fa-cloud-upload-alt"></i> Select Image/Video
                                </label>
                                <p style="font-size: 14px;">You can only send 4 files. Discard if you dont have any multimedia content to send</p>
                            </div>
                            <div>
                                <img class="image_view_carosal" src="" alt="" style="width:100px;" id="userView">
                                <input class=" file_upload_input_box" type="file" id="askImage" name="askImage" accept="image/*,video/*" multiple onchange="viewpImage(event)">
                            </div>
                        </div>

                        <button class="upload_post_submit btn" type="submit">Send</button>
                    </form>
                </div>
            </div>
        </div>

<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script>
           function viewpImage(event) {
            var input = event.target;
            var fileLimit = 4; // Maximum number of files

            // Clear previous previews
            document.getElementById('previewContainer').innerHTML = "";
            document.getElementById('userView').src = "";

            if (input.files.length > fileLimit) {
                alert("You can only select up to " + fileLimit + " files at once.");
                input.value = "";
                return;
            }

            var carouselInner = document.createElement("div");
            carouselInner.className = "carousel-inner";

            for (var i = 0; i < input.files.length; i++) {
                var carouselItem = document.createElement("div");
                carouselItem.className = i === 0 ? "carousel-item active" : "carousel-item";

                // Set a fixed height for carousel items
                carouselItem.style.height = "300px"; // Adjust the height as needed

                if (input.files[i].type.includes("video")) {
                    var videoPreview = document.createElement("video");
                    videoPreview.classList.add("video-preview_popup");
                    videoPreview.controls = true;
                    var source = document.createElement("source");
                    source.src = URL.createObjectURL(input.files[i]);
                    source.type = input.files[i].type;

                    videoPreview.appendChild(source);
                    carouselItem.appendChild(videoPreview);
                } else {
                    var imgPreview = document.createElement("img");
                    imgPreview.src = URL.createObjectURL(input.files[i]);
                    imgPreview.classList.add("image-preview_popup"); 
                    carouselItem.appendChild(imgPreview);
                }

                carouselInner.appendChild(carouselItem);
            }

            document.getElementById('previewContainer').appendChild(carouselInner);
        }

        function resizeTextarea(textarea) {
            textarea.style.height = "auto";
            textarea.style.height = (textarea.scrollHeight) + "px";
        }
    </script>
   
